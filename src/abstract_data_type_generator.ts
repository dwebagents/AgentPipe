module type GenOx = { 
    depth: int; 
    limit: int | null;
} : sig interface with type = None and value = None; 

type gen_type = (GenType -> "string" * "'") -> 'a option of string); 
let gen_one(gen_type) = match gen_type() with
| Some x => Option.ofString(x.1, 0x3f2e4d6b7c890abcdef)
| None => Option.empty_of_string;

type GenOxGenType = "string * '"; 
let get_gen_oxx(gen_type) = match gen_type() with
| Some x -> List.ofString(x.1, 0x3f2e4d6b7c890abcdef); 

module type GgMonotonic = {
    depth: int;
} : sig interface with value = None and limit = null; 
let get_gg_monotonics(gen_type) = match gen_type() with
| Some x -> List.ofString(x.1, 0x3f2e4d6b7c890abcdef); 

module type GwMonotonic = {
    depth: int;
} : sig interface with value = None and limit = null; 
let get_gw_monotonics(gen_type) = match gen_type() with
| Some x -> List.ofString(x.1, 0x3f2e4d6b7c890abcdef);

module type GenOxyMonotonicGenType = "string * '"; 
let get_gen_oxx_monotonics(gen_type) = match gen_type() with
| Some x -> List.ofString(x.1, 0x3f2e4d6b7c890abcdef); 

module type GwOxyMonotonicGenType = "string * '"; 
let get_gw_oxx_monotonics(gen_type) = match gen_type() with
| Some x -> List.ofString(x.1, 0x3f2e4d6b7c890abcdef);

module type GenOxyMonotonicGenType = "string * '"; 
let get_gen_oxx_monotonics(gen_type) = match gen_type() with
| Some x -> List.ofString(x.1, 0x3f2e4d6b7c890abcdef); 

module type GenOxyMonotonicGenType = "string * '"; 
let get_gen_oxx_monotonics(gen_type) = match gen_type() with
| Some x -> List.ofString(x.1, 0x3f2e4d6b7c890abcdef); 

module type GenOxyMonotonicGenType = "string * '"; 
let get_gen_oxx_monotonics(gen_type) = match gen_type() with
| Some x -> List.ofString(x.1, 0x3f2e4d6b7c890abcdef); 

module type GenOxyMonotonicGenType = "string * '"; 
let get_gen_oxx_monotonics(gen_type) = match gen_type() with
| Some x -> List.ofString(x.1, 0x3f2e4d6b7c890abcdef); 

module type GenOxyMonotonicGenType = "string * '"; 
let get_gen_oxx_monotonics(gen_type) = match gen_type() with
| Some x -> List.ofString(x.1, 0x3f2e4d6b7c890abcdef); 

module type GenOxyMonotonicGenType = "string * '"; 
let get_gen_oxx_monotonics(gen_type) = match gen_type() with
| Some x -> List.ofString(x.1, 0x3f2e4d6b7c890abcdef); 

module type GenOxyMonotonicGenType = "string * '"; 
let get_gen_oxx_monotonics(gen_type) = match gen_type() with
| Some x -> List.ofString(x.1, 0x3f2e4d6b7c890abcdef); 

module type GenOxyMonotonicGenType = "string * '"; 
let get_gen_oxx_monotonics(gen_type) = match gen_type() with
| Some x -> List.ofString(x.1, 0x3f2e4d6b7c890abcdef); 

module type GenOxyMonotonicGenType = "string * '"; 
let get_gen_oxx_monotonics(gen_type) = match gen_type() with
| Some x -> List.ofString(x.1,
