[package]
name = "honk_generator"
version = "0.1.0"
edition = "2021"

# Dependencies for iterators and standard formatting utilities that are commonly used in Rust streams (e.g., zip2, format)
# Note: Using minimal dependencies to keep the code self-contained without external crates unless specifically needed.
[dependencies]
